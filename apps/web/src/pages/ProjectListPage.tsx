/**
 * 桌面端思维导图列表页 —— 骨架 stub。
 *
 * TODO: 接入 SqlProjectRepo + FileWatcher，展示本地 .zmind 文件；
 * 缺失文件显示失效卡片；保留文件夹侧栏（虚拟 tag 版）。
 */
export function ProjectListPage() {
  return (
    <div className="p-8">
      <h1 className="mb-2 text-2xl font-semibold">思维导图</h1>
      <p className="text-muted-foreground">列表页骨架 — 数据层待接入 SQLite + fs。</p>
    </div>
  )
}
