/**
 * 测试用例生成策略
 */

export const generationStrategy = (): string => `# 生成策略

## 模块怎么拆：以 UI 元素为锚点

**禁止按功能分类建模块**（如"发布动态""互动""查看与权限"）。必须按需求中涉及的**实际 UI 元素**建模块。

嵌套拆分规则（从外到内，逐层深入）：

1. **界面/页面** → 拆出界面上的各个区域和独立元素
2. **复合组件**（卡片、表单、列表项、工具栏等）→ 继续拆出组件内部的子元素
3. **按钮/操作触发新界面**（弹窗、抽屉、新页面）→ 新界面作为该按钮的子节点，继续按第 1 步拆
4. **通用行为**（打开/关闭/权限/空状态）→ 单独建一个子节点承载，不直接挂在父级下

拆到什么程度停：叶子节点是一个**不可再拆的单一交互元素**（按钮、输入框、选择器、开关等）。

**命名规则：直接用 UI 元素名称命名**（如"昵称输入框""保存按钮""裁剪弹窗""动态卡片"），禁止加"模块"二字。

## 用例怎么写：按层级分工

- **复合组件**（弹窗、表单、页面等有子元素的）：验证 UI 展示（布局、文案、空状态、加载态）和组件自身逻辑（打开/关闭、权限、状态切换）。
- **单一元素**（按钮、输入框、选择器等叶子节点）：只验证交互逻辑，不重复验证父级已覆盖的 UI 展示。凡是会触发请求或状态变更的元素（提交按钮、保存、删除、发送等），必须覆盖网络异常、超时、重复触发、权限不足等场景。

## 粒度

- 重点在**模块拆分够细**，模块拆到位了用例自然不会差。
- 超过 10 个用例 → 说明该继续拆子模块。
- 覆盖全、不重复：刚好覆盖该元素的所有场景。
- **深度目标**：模块嵌套通常 2-4 层。只有 1 层说明分析太浅。

## 模块嵌套规则

- 模块严格按 UI 包含关系嵌套，禁止脱离上下文放到外面或同级
- 按钮触发的弹窗 → 弹窗是该按钮的子模块
- **一个模块只对应一个 UI 元素**，禁止把多个元素合并命名（如"评论/点赞按钮""输入框与提交按钮"）。每个元素独立建模块。
- **模块和用例不能混放**：一个模块下要么只有子模块，要么只有用例，不能同时存在。有子模块的模块需要把通用逻辑（如打开/关闭/权限）另建子模块承载；有用例的模块下禁止再建子模块。

✅ 正确示例（注意多层嵌套和命名）：
\`\`\`
朋友圈首页                             ← 第 1 层：界面
  ├─ 页面通用行为                      ← 通用逻辑（下拉刷新、加载更多、空状态）
  ├─ 顶部导航栏                        ← 第 2 层：复合组件
  │   ├─ 返回按钮                      ← 第 3 层：单一元素
  │   └─ 相机按钮                      ← 点击触发新界面 ↓
  │       └─ 发布编辑页                ← 第 3 层：按钮触发的新界面
  │           ├─ 编辑页通用行为        ← 通用逻辑（取消、草稿保存）
  │           ├─ 文本输入框            ← 第 4 层：单一元素
  │           ├─ 图片上传按钮
  │           │   └─ 图片编辑弹窗      ← 继续嵌套
  │           ├─ 位置选择器
  │           └─ 发布按钮
  ├─ 动态卡片                          ← 第 2 层：复合组件（列表中的卡片）
  │   ├─ 卡片通用行为                  ← 通用逻辑（展示、长按菜单）
  │   ├─ 头像
  │   ├─ 图片九宫格
  │   ├─ 点赞按钮
  │   └─ 评论按钮
  │       └─ 评论输入弹窗              ← 按钮触发的新界面
  │           ├─ 评论输入框
  │           └─ 发送按钮
  └─ 搜索框
\`\`\`

❌ 绝对禁止：
\`\`\`
朋友圈
  ├─ 发布动态                ← 错！功能分类，不是 UI 元素
  ├─ 互动(评论/点赞)         ← 错！多个元素合并
  └─ 查看与权限              ← 错！太笼统
\`\`\`
`
